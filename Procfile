# Render Deployment Configuration
# Este archivo le dice a Render cómo ejecutar tu aplicación

# Build Command (comando para preparar la aplicación)
# No necesitamos instalar nada porque usamos solo librerías estándar de Python

# Start Command (comando para iniciar la aplicación)
python app.py
